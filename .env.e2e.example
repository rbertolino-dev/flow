# Copie para .env.e2e.local (NUNCA commite o arquivo .local)
# cp .env.e2e.example .env.e2e.local && chmod 600 .env.e2e.local
#
# Conta de teste dedicada (não use produção/admin).

E2E_EMAIL=
E2E_PASSWORD=

# Opcional: força organization_id ativa (localStorage) quando a conta tem várias orgs
# e a default não tem leads no funil.
# E2E_ORG_ID=
